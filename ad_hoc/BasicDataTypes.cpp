#include <bits/stdc++.h>
using namespace std;

int main(void) {
    int entero;
    long long largo;
    char caracter;
    float real;
    double real_largo;

    cin >> entero;
    cin >> largo;
    cin >> caracter;
    cin >> real;
    cin >> real_largo;

    cout << entero << endl;
    cout << largo << endl;
    cout << caracter << endl;
    cout << fixed << setprecision(10) << real << endl;
    cout << fixed << setprecision(10) << real_largo << endl;
    return 0;
}
